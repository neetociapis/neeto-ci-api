---
title: "List all available slots"
openapi: "/bundled/slots.yaml GET /slots/{meeting_slug}"
---

import SubdomainInfo from "/snippets/subdomain-info.mdx";

This API allows to get a list of available slots of the host (or hosts in the case of multi-host or round-robin meetings) for a specific scheduling link for a given month. These slots can later be used to book a meeting.

<SubdomainInfo />
